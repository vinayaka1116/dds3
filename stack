#include<iostream>
#include<stack>
using namespace std;

int main (){
   stack<int>st;
     st.push(6);
     st.push(16);
     st.push(5);
     st.push(9);
      st.push(1);
   
   while(!st.empty()){
       cout<<st.top()<<endl;
       st.pop();
   }
   
   
}
